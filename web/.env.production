# Production build config: the SPA is served from the Spring Boot app's own
# static resources at the root path (single-origin deployment via Docker),
# so both the Vite base path and the API base URL are root-relative.
VITE_BASE_PATH=/
VITE_API_BASE_URL=/api
